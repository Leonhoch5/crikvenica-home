# Server
HOST=0.0.0.0
PORT=1337

# Secrets — generate each one with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
APP_KEYS=key1base64,key2base64
API_TOKEN_SALT=
ADMIN_JWT_SECRET=
TRANSFER_TOKEN_SALT=
JWT_SECRET=
ENCRYPTION_KEY=

# CORS — set to the public URL of your Next.js frontend
FRONTEND_URL=https://yourdomain.com
